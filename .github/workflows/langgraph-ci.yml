name: langgraph-ci

on:
  push:
    paths:
      - "langgraph/**"
      - ".github/workflows/langgraph-ci.yml"
  pull_request:
    paths:
      - "langgraph/**"
      - ".github/workflows/langgraph-ci.yml"

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: langgraph
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: langgraph/pyproject.toml
      - name: Install
        run: python -m pip install -e ".[dev]"
      - name: Lint
        run: ruff check .
      - name: Test
        run: pytest -q
